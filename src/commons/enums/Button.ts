enum ButtonTypes {
    Submit = "submit",
    Reset = "reset",
    Button = "button"
}

enum ButtonVariants {
    Primary = "primary",
    Error = "error",
    Neutral = "neutral"
}

export {
    ButtonTypes,
    ButtonVariants
}